name: example
description: A new Flutter project.

version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  kkiapay_flutter_sdk:
    git:
      url: https://github.com/Shek863/kkiapay-flutter-sdk
      ref: master # branch name


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true